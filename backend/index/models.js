const { User } = require('../models/user');
const { WorkPermit } = require('../models/workpermit');
const { BusinessPermit } = require ('../models/businesspermit');

module.exports = { User, WorkPermit, BusinessPermit};